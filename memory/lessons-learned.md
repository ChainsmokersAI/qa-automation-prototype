# Lessons Learned

<!-- 형식: [날짜] 상황 → 문제 → 해결 → 교훈 -->

[2026-04-09] skill 이름 짓기 → "learn"이 너무 추상적이라 PM이 거부 → "learn-context"로 변경 → skill 이름은 행위 + 대상을 조합하여 구체적으로 짓는다

[2026-04-09] CLAUDE.md 초안 작성 → 컨텍스트가 무엇인지 설명 없이 규칙만 나열 → PM이 "Claude가 알아듣게 상세하게" 피드백 → CLAUDE.md는 규칙 나열이 아니라 각 개념을 충분히 설명해야 한다. 200줄 제한 내에서 여유롭게 작성할 것

[2026-04-09] 영문명 직역 시도 (Target Audience, Guardrails 등) → PM이 이전 버전이 나았다고 피드백 → 제품 용어는 자연스러움보다 일관성과 직관성 우선. 변경 시 PM에게 먼저 확인

[2026-04-09] 시나리오에 중요도 필드를 추가했으나, TC skill에서 정의 없이 참조 → PM이 바로 지적 → skill 간 의존하는 필드는 양쪽에서 일관되게 정의해야 한다

[2026-04-09] 작업 완료 후 메모리 업데이트를 하지 않음 → PM이 직접 요청 → 작업 완료 후 메모리 업데이트는 PM이 말하기 전에 에이전트가 먼저 제안해야 한다

[2026-04-10] 파이프라인 전체 흐름을 CLAUDE.md에 명시하지 않고 개별 스킬만 정의 → PM이 CLAUDE.md에 파이프라인 워크플로우 섹션 추가 피드백 → 에이전트가 단계 간 의존성(입력/출력/전제 조건)을 명확히 이해하려면 전체 흐름을 한곳에 정리해야 한다

[2026-04-10] CLAUDE.md 규칙 강화 작업에서 섹션을 늘려 200줄을 초과함 → PM이 "CLAUDE.md는 항상 200줄 이내로 compact하게 유지" 피드백 → **CLAUDE.md는 200줄 이내 엄수**. 핵심 원칙·판정 기준·금지 규칙만 기록하고, 장황한 부연설명·중복 예시·자주 발생하는 오분류 나열 등은 SKILL.md나 lessons-learned.md로 이관한다. CLAUDE.md에 내용을 추가할 때는 "이 줄이 없으면 에이전트가 틀릴까?"를 먼저 확인한다.

[2026-04-13] context 항목 구조 리팩토링 중 User Flows 순서를 4번째로 이동했으나 CLAUDE.md 메인 테이블만 반영하고 learn-context/SKILL.md의 Phase 3 웹 리서치 테이블과 knowledge/ 리서치 범위 테이블에는 반영하지 못함 → PM이 추가 지적 → **enumeration을 건드리는 구조 변경(순서 변경·항목 추가/삭제)은 grep으로 모든 열거 위치를 먼저 찾아야 한다**. 스캔 시 "필수/선택 같은 라벨"뿐 아니라 "동일 항목명 집합"도 검사 대상이다. 같은 항목을 여러 테이블·섹션·파일 경로 리스트가 공유할 수 있으므로 한 곳만 고치면 일관성이 깨진다.

[2026-04-14] learn-context 웹 리서치 개선 요청에서 PM이 "Phase별 5개 이내로 줄이고 싶다"고 했을 때, "검색 테이블의 행(검색 대상)을 8→5개로 축소"로 해석하여 플랜 초안 작성 → PM이 "검색 대상이 아니라 실제 수집 소스(URL) 수를 줄이려는 의도"라고 정정 → **PM 요청의 "줄인다"는 표현은 대상이 모호할 수 있다**. 특히 웹 리서치·수집 과정에는 "검색 쿼리 수", "검색 대상 카테고리 수", "실제 수집 소스 수", "knowledge 파일 수" 등 여러 축이 공존한다. "무엇을 줄이는가"의 대상을 플랜 작성 전에 한 번 명확히 확인해야 한다. 요청 해석 시 축이 여러 개라고 의심되면, 플랜 초안으로 넘어가기 전에 AskUserQuestion으로 먼저 축을 정렬한다.

[2026-04-14] learn-context 구조 리팩토링 — 웹 리서치 결과를 `knowledge/{slug}.md` 중간층에 저장하던 구조를 raw URL을 context/에 직접 인용하는 구조(`[추측: URL (YYYY-MM-DD)]`)로 전환 → 중간층 내용이 context/ 항목과 거의 동일해 불필요한 간접 참조를 만들었기 때문 → **교훈**: 근거의 원천이 raw URL이라면 중간 저장층은 재포장일 뿐이다. 파생 파일/디렉토리를 설계할 때는 "이 계층이 원본을 재포장하는 것 이상의 가치를 주는가"를 먼저 자문한다. 재현성(검색 키워드 등)이 핵심 가치가 아닐 경우 raw 참조가 더 낫다.

[2026-04-14] run-simulation 스킬 수정 중 병렬 실행 규칙 섹션 Edit 직후, PM이 중간 상태를 보고 "파일이 날아간 것 같다"고 판단해 다음 Edit(에러 처리 재작성)를 reject → 이후 Read로 대조하니 해당 한 섹션만 빠진 상태였음 → **교훈**: 연속 Edit로 여러 섹션을 추가할 때 PM이 중간 상태를 보고 혼란스러워할 수 있다. (1) 큰 변경은 한 번의 Edit으로 몰아넣거나, (2) 변경 범위를 먼저 선언하고 순차 진행함을 명시하면 혼란이 줄어든다. PM이 "날아간 것 같다"고 말하면 **즉시 Read로 현재 파일 상태를 대조**하고, 빠진 부분만 정확히 짚어 복구 범위를 최소화한다.

[2026-04-14] `/run-simulation` TC 실행 규칙을 "TC 원본 메시지 그대로 전송"에서 "실제 응답 맥락에 맞춰 re-anchor"로 전환 → PM이 "억지로 맞추지 말고 자연스럽게 이어가되 비고에 기록"을 요청 → **교훈**: 결정성(determinism) vs 현실성(fidelity)이 충돌할 때, 결정성을 포기하는 대신 **차이를 추적 가능한 방식으로 기록**하면 양쪽을 절충할 수 있다. `사용자 메시지(TC 원본)` / `사용자 메시지(실전송)` / `비고`를 3필드로 분리한 것이 핵심. 원본·실전송을 양쪽 다 남기면 조정 사실 자체가 데이터가 되어 이후 평가·분석 단계에서 활용 가능하다.

[2026-04-14] 병렬 실행·Rate Limit 재시도 규칙을 신규 추가할 때 기본값을 "보수적 값 + 자동 하향 규칙" 조합으로 설계 → 동시성 기본 5, Rate Limit 명시 시 한도 50%, 429 반복 시 동적 절반 하향 → **교훈**: PM이 개발자가 아닌 환경에서 튜닝 파라미터를 노출하면 인지 부담이 된다. **"안전한 기본값 + 자동 적응 규칙"을 SKILL.md에 박아두면 PM 개입 없이도 적절히 동작**한다. 단, PM이 명시적으로 조정할 수 있는 창구(실행 전 요약에서 동시 실행 수 표시·조정 가능)는 남겨둔다 — 자동화와 수동 오버라이드는 양립한다.
